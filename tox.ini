[tox]
envlist =
    {py35,py36,py37}
    coverage
[testenv]
deps =
    -rrequirements_dev.txt
commands = nosetests

[testenv:coverage]
deps =
    -rrequirements_dev.txt
commands =
    nosetests --with-coverage --cover-package=. --cover-min-percentage=90 --cover-erase
    coveralls